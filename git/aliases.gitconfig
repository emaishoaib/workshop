[alias]
    rbi = !git rebase -i $(git merge-base HEAD origin/$(git remote show origin | grep 'HEAD branch' | awk '{print $NF}'))
    blog = !git log --oneline HEAD ^origin/$(git remote show origin | grep 'HEAD branch' | awk '{print $NF}')